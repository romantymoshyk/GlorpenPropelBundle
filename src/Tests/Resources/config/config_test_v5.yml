imports:
    - { resource: "@GlorpenPropelBundle/Resources/config/config.yml" }

framework:
    secret:          "test secret"
    test: ~
    session:
        storage_id: session.storage.mock_file
propel:
  path: "%kernel.project_dir%/vendor/propel/propel1"
  phing_path: "%kernel.project_dir%/vendor/phing/phing"
